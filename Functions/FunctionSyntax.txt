		Синтаксис написания функции
type name(prameters)
{
	....;
	group-of-statements;
	....;
}
type - тип возвращаемого значения.
void - пустота.
return - прерывает работу функции и возвращает значение на место вызова.
return value;//value - это значение, которое return вернет на место вызова.